<configuration name="modules.conf" description="Modules">
  <modules>
    <!-- Loggers -->
    <load module="mod_console"/>
    <load module="mod_logfile"/>

    <!-- XML Interfaces -->
    <!-- mod_xml_curl intentionally not loaded — unused in this bootstrap
         deployment. It's the seam a future Configuration Service would use
         (see ../../../ARCHITECTURE.md §4b), not something wired up here. -->
    <load module="mod_xml_cdr"/>

    <!-- Event Handlers -->
    <load module="mod_event_socket"/>
    <load module="mod_json_cdr"/>

    <!-- Endpoints -->
    <load module="mod_sofia"/>

    <!-- Applications -->
    <load module="mod_commands"/>
    <load module="mod_curl"/>
    <load module="mod_db"/>
    <load module="mod_dptools"/>
    <load module="mod_expr"/>
    <load module="mod_fifo"/>
    <load module="mod_hash"/>
    <load module="mod_audio_stream"/>

    <!-- Database Interfaces -->
    <load module="mod_pgsql"/>

    <!-- Dialplan Interfaces -->
    <load module="mod_dialplan_xml"/>

    <!-- Codec Interfaces -->
    <load module="mod_spandsp"/>
    <load module="mod_opus"/>

    <!-- File Format Interfaces -->
    <load module="mod_sndfile"/>
    <load module="mod_native_file"/>
    <load module="mod_local_stream"/>
    <load module="mod_tone_stream"/>
  </modules>
</configuration>
